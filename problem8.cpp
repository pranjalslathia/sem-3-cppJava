#include <iostream>
#include <vector>

int main() {
    std::vector<int> numbers = {1, 2, 3, 4, 5, 6, 7};
    int *reverse = new int[numbers.size()]
    for (int i = 0; i < numbers.size(); i++){
        if (max < numbers[i]){
            max_2 = max;
            max = numbers[i];
            
        }
    }
    std::cout << "Max: " << max << " | Second Max: " << max_2 << std::endl;
    return 0;
}